# use case

对着这份模板写目标仓的 `skills/temp-skill/<skill-name>/use-case.md`。不要把模板本身拷进 `temp-skill/`。操作和对照按本 skill「表达规定」写：分类、分点、或表格。

一条 use case：完成一个具体目标而进行的一组操作，并得到可观察结果。不是「第几步」，也不是注意事项。

每条用二级标题当名字。正文固定三块，行首字面量保持「目标：」「操作：」「可观察结果：」。

## 列出已装的 skill
目标：看到本机全局装了哪些 skill
操作：
- 跑 `npx skills ls -g`
- 看输出里的名字和路径
可观察结果：`npx skills ls -g` 列出了已装的名字

## <下一条 use-case-名字>
目标：<一句话，做成了什么>
操作：
- <动作一>
- <动作二>
可观察结果：<谁能看见什么文件或状态，才算这条做完>
